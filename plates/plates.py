def main():
    plate = input("Plate: ")
    if is_valid(plate):
        print("Valid")
    else:
        print("Invalid")


def is_valid(s):
    letter_mode = False
    for char in s:
        if type(char) == int()
    if s[0].isalpha() and s[1].isalpha() and 2 <= len(s) <= 6:


main()